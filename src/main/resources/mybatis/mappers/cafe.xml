<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cafe">

	<insert id="addCafe" parameterType="com.nextroom.vo.CafeVo">
		<![CDATA[
			insert into cafe
			values (cafeNo.nextval, 
					#{userNo}, 
					#{cafeName}, 
					#{cafeHp}, 
					#{openTime}, 
					#{closeTime}, 
					#{cafeIntro}, 
					#{url}, 
					#{cafeImgPath}, 
					#{address}, 
					#{sido}, 
					#{sidoDetail})
		]]>
	</insert>




	<update id="updateCafe" parameterType="com.nextroom.vo.CafeVo">
	
	<![CDATA[
			update cafe
			set cafeImg = #{cafeImg},
				
			where cafeNo = #{no}
		]]>

	</update>


	<insert id="addCafeKey" parameterType="com.javaex.vo.CategoryVo">
		<selectKey keyProperty="cafeNo" resultType="int" order="BEFORE">
			select cafeNo.nextval from dual
		</selectKey>
	<![CDATA[	
		insert into cafe
			values (cafeNo.nextval, 
					#{userNo}, 
					#{cafeName}, 
					#{cafeHp}, 
					#{openTime}, 
					#{closeTime}, 
					#{cafeIntro}, 
					#{url}, 
					#{cafeImgPath}, 
					#{address}, 
					#{sido}, 
					#{sidoDetail})
    	]]>
	</insert>

	<!-- <insert id="addInteriorImg" parameterType="com.nextroom.vo.CafeVo"> <![CDATA[ insert into interiorImg values (cafeNo.nextval, #{userNo}, #{cafeName}, 
		#{cafeHp}, #{openTime}, #{closeTime}, #{cafeIntro}, #{url}, #{cafeImg}, #{address}, #{sido}, #{sidoDetail}) ]]> </insert> -->

</mapper>